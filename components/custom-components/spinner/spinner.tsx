import { Spinner as NextSpinner } from "@nextui-org/spinner";

const Spinner = () => {
  return (
    <div className="flex w-full justify-center items-center h-full">
      <NextSpinner color="default" />
    </div>
  );
};

export default Spinner;
